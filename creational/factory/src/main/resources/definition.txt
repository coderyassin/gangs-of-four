Define an interface for creating an object, but let subclasses decide which
class to instantiate. It allows for object creation to be delegated, providing
flexibility in determining the actual product type.